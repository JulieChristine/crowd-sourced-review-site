
import { occupationCost } from 'crowd-sourced-review-site/helpers/occupation-cost';
import { module, test } from 'qunit';

module('Unit | Helper | occupation cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = occupationCost([42]);
  assert.ok(result);
});

