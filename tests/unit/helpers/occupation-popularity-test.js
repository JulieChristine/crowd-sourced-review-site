
import { occupationPopularity } from 'crowd-sourced-review-site/helpers/occupation-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | occupation popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = occupationPopularity([42]);
  assert.ok(result);
});

